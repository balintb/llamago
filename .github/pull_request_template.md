<!-- The changelog is generated from commit subjects, so write this for the reviewer instead. -->

## What this changes

## Why

## Checks

- [ ] `go test -short ./...` passes
- [ ] `go vet ./...` is clean
- [ ] Commit subjects are conventional (`feat:`, `fix:`, `docs:` …). `git config core.hooksPath .githooks` checks them as you commit
- [ ] `go test ./...` run against a live Ollama, or a note below saying why not

## Screenshots

<!-- Anything that changes what is on screen: a screenshot or a paste of the frame. Layout bugs are hard to see in a diff. -->
